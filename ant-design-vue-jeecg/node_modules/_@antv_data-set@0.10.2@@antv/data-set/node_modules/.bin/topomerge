#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_topojson-client@3.0.1@topojson-client/bin/topomerge" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_topojson-client@3.0.1@topojson-client/bin/topomerge" "$@"
  ret=$?
fi
exit $ret
